[package]
name = "pystral_physics"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = { version = "0.30", features = ["serde"] }
rapier3d = "0.34.0"
serde = { version = "1.0", features = ["derive"] }

[lints]
workspace = true

[package]
name = "pystral_core"
version = "0.1.0"
edition = "2024"

[dependencies]
hexx = { version = "0.24.0", features = ["grid", "serde", "mesh"] }
glam = { version = "0.30", features = ["serde"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
# tween = "2.2.0" # Not directly used in core yet, but if it is, add it.

// Test-only fixture — loaded by tests/playwright/casualty_boundary.spec.js; never in manifest.json.
// Deterministic Draw outcome overlay for the canonical pg_rpg browser gate.
// @include "/scenarios/skirmish.rhai"
sim.set_agent_health(1, 0);
sim.set_agent_health(2, 0);
sim.set_agent_health(3, 0);
sim.set_agent_health(4, 0);
sim.set_agent_health(5, 0);
sim.set_agent_health(6, 0);
